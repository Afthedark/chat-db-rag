# ============================================
# Chat DB RAG - Configuration
# ============================================

# Server Configuration
PORT=3000
NODE_ENV=development

# ============================================
# AI Provider Configuration
# Options: 'ollama' or 'openrouter'
# ============================================
AI_PROVIDER=ollama

# --- OpenRouter Configuration (if AI_PROVIDER=openrouter) ---
OPENROUTER_API_KEY=your_openrouter_api_key_here
OPENROUTER_MODEL=meta-llama/llama-3-70b-instruct

# --- Ollama Local Configuration (if AI_PROVIDER=ollama) ---
OLLAMA_URL=http://localhost:11434/api/chat
OLLAMA_MODEL=gemini-3-flash-preview:cloud

# --- Gemini Configuration Switch ---
# Set to 'true' to enable Gemini-specific optimizations (1M context)
# This increases token budgets for better rule compliance
# Default is 'false' for backwards compatibility
USE_GEMINI_OPTIMIZATIONS=true

# ============================================
# Memory Database Configuration (Sequelize)
# This is the database where the app stores
# chat history, rules, and connection configs
# ============================================
MEM_DB_HOST=localhost
MEM_DB_PORT=3301
MEM_DB_USER=root
MEM_DB_PASS=
MEM_DB_NAME=ai_memory_db

# Ollama Performance Tuning
# For 128K models (llama3.1:8b): 8192
# For 1M models (Gemini): 128000 or higher
OLLAMA_NUM_CTX=128000
OLLAMA_TEMPERATURE=0.1
OLLAMA_NUM_PREDICT=500
